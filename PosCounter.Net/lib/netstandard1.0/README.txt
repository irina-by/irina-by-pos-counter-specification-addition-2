System.ValueTuple.dll 4.5.0 (netstandard1.0) — запасная копия для сборки net452 без NuGet.

Если dotnet restore не скачал пакет (длинный путь OneDrive, офлайн), MSBuild берёт DLL отсюда.

При NETLOAD в AutoCAD рядом с PosCounter.Net.dll должна лежать копия System.ValueTuple.dll
(скрипт build-ac2016.cmd копирует в dll 2016\).
